{
  "name": "kappa-brain",
  "version": "1.0.0",
  "description": "kappa-brain — MCP server for Kappa Cell memory: 23 tools, FTS5, supersede system, meditation ops",
  "type": "module",
  "bin": {
    "kappa-brain": "./src/server.ts"
  },
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "start": "bun dist/server.js",
    "test": "bun test test/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "hono": "^4.12.5",
    "@hono/node-server": "^1.13.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}